import { Box } from "@mui/material";
import TodoList from "./TodoList";

export default function TodoPage() {
    return (
        <Box>
            <h1>Todos</h1>
            <TodoList />
        </Box>
    );
}
